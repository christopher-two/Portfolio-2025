{
  "name": "christopher-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler types && astro dev --port 9002",
    "build": "wrangler types && astro check && astro build",
    "preview": "astro preview",
    "check": "wrangler types && astro check",
    "typecheck": "wrangler types && astro check",
    "deploy": "pnpm build && wrangler deploy",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.3",
    "astro": "^7.1.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.112.0"
  }
}
